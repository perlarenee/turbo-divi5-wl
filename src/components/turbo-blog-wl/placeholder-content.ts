// Divi dependencies.
import {placeholderContent as placeholder} from '@divi/module-utils';

// Local dependencies.
import { TurboBlogWlAttrs } from './types';


export const placeholderContent: TurboBlogWlAttrs = {
  title: {
    innerContent: {
      desktop: {
        value: placeholder.title,
      }
    }
  },
};
